---
import { HiOutlineBell, HiOutlineCalendarDays, HiOutlineArrowTopRightOnSquare, HiOutlineExclamationTriangle, HiOutlineCheckCircle } from "react-icons/hi2";

interface Props {
  type?: "update" | "alert" | "success" | "important";
  badgeText?: string;
  title: string;
  message?: string;
  lastUpdated?: string;
  links?: {
    text: string;
    href: string;
    isPrimary?: boolean;
    download?: boolean;
  }[];
}

const {
  type = "update",
  badgeText = "UPDATE",
  title,
  message,
  lastUpdated,
  links = []
} = Astro.props;

// Determine colors based on type
let themeClasses = {
  container: "bg-blue-500/5 border-blue-500/20 hover:border-blue-500/40 shadow-blue-500/5",
  badge: "bg-blue-500/10 text-blue-500 border-blue-500/20",
  iconBg: "bg-blue-500/10 text-blue-500",
  linkPrimary: "bg-blue-500 hover:bg-blue-600 text-white shadow-blue-500/20",
  linkSecondary: "bg-blue-500/10 hover:bg-blue-500/20 text-blue-600 dark:text-blue-400 border-blue-500/20"
};

if (type === "alert") {
  themeClasses = {
    container: "bg-rose-500/5 border-rose-500/20 hover:border-rose-500/40 shadow-rose-500/5",
    badge: "bg-rose-500/10 text-rose-500 border-rose-500/20",
    iconBg: "bg-rose-500/10 text-rose-500",
    linkPrimary: "bg-rose-500 hover:bg-rose-600 text-white shadow-rose-500/20",
    linkSecondary: "bg-rose-500/10 hover:bg-rose-500/20 text-rose-600 dark:text-rose-400 border-rose-500/20"
  };
} else if (type === "success") {
  themeClasses = {
    container: "bg-emerald-500/5 border-emerald-500/20 hover:border-emerald-500/40 shadow-emerald-500/5",
    badge: "bg-emerald-500/10 text-emerald-500 border-emerald-500/20",
    iconBg: "bg-emerald-500/10 text-emerald-500",
    linkPrimary: "bg-emerald-500 hover:bg-emerald-600 text-white shadow-emerald-500/20",
    linkSecondary: "bg-emerald-500/10 hover:bg-emerald-500/20 text-emerald-600 dark:text-emerald-400 border-emerald-500/20"
  };
} else if (type === "important") {
  themeClasses = {
    container: "bg-indigo-500/5 border-indigo-500/20 hover:border-indigo-500/40 shadow-indigo-500/5",
    badge: "bg-indigo-500/10 text-indigo-500 border-indigo-500/20",
    iconBg: "bg-indigo-500/10 text-indigo-500",
    linkPrimary: "bg-indigo-500 hover:bg-indigo-600 text-white shadow-indigo-500/20",
    linkSecondary: "bg-indigo-500/10 hover:bg-indigo-500/20 text-indigo-600 dark:text-indigo-400 border-indigo-500/20"
  };
}
---

<div class={`not-prose flex flex-col p-4 sm:p-5 border rounded-2xl transition-all duration-300 shadow-sm ${themeClasses.container}`}>
  <!-- Main Content Row -->
  <div class="flex items-start gap-3.5">
    <!-- Icon Container -->
    <div class={`flex items-center justify-center p-2.5 rounded-xl shrink-0 ${themeClasses.iconBg}`}>
      {type === "update" && <HiOutlineBell className="text-lg animate-bounce" />}
      {type === "alert" && <HiOutlineExclamationTriangle className="text-lg animate-pulse" />}
      {type === "important" && <HiOutlineCalendarDays className="text-lg animate-pulse" />}
      {type === "success" && <HiOutlineCheckCircle className="text-lg" />}
    </div>

    <!-- Details -->
    <div class="space-y-1.5 flex-1 min-w-0">
      <!-- Row containing Badge & Last Updated -->
      <div class="flex items-center gap-2 flex-wrap text-[0.65rem] sm:text-xs">
        <span class={`px-2 py-0.5 rounded-md font-bold uppercase tracking-wider border select-none text-[0.6rem] ${themeClasses.badge}`}>
          {badgeText}
        </span>
        {lastUpdated && (
          <span class="text-text-muted select-none flex items-center gap-1 font-semibold">
            • Updated: {lastUpdated}
          </span>
        )}
      </div>

      <!-- Title -->
      <h3 class="text-sm sm:text-base text-text-main font-extrabold leading-snug m-0">
        {title}
      </h3>

      <!-- Message -->
      {message && (
        <p class="text-[0.72rem] sm:text-xs text-text-muted leading-relaxed m-0 max-w-[650px]">
          {message}
        </p>
      )}

      <!-- Links & Call to Actions (bottom left with spacing) -->
      {links && links.length > 0 && (
        <div class="flex flex-wrap items-center gap-2.5 pt-2.5">
          {links.map((link) => (
            <a
              href={link.href}
              target={link.download ? undefined : "_blank"}
              rel={link.download ? undefined : "noopener noreferrer"}
              download={link.download || undefined}
              class={`flex items-center justify-center gap-1.5 px-4 py-2 text-[0.68rem] font-bold uppercase tracking-wider rounded-lg transition-all shadow-xs text-center select-none ${link.isPrimary ? `btn-primary ${themeClasses.linkPrimary}` : `border border-solid ${themeClasses.linkSecondary}`}`}
            >
              <span>{link.text}</span>
              {!link.download && <HiOutlineArrowTopRightOnSquare className="text-xs shrink-0 transition-transform duration-300 group-hover:translate-x-0.5 group-hover:-translate-y-0.5" />}
            </a>
          ))}
        </div>
      )}
    </div>
  </div>
</div>
